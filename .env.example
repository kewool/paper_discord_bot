# npm run demo works without credentials. Copy this file to .env for Discord operation.
NODE_ENV=development
DEMO_MODE=false
HOST=127.0.0.1
PORT=3000
PUBLIC_URL=http://localhost:3000
DATA_DIR=data/production
TRUST_PROXY_HOPS=0

# Discord Developer Portal application. Never paste secrets into chat.
DISCORD_CLIENT_ID=
DISCORD_BOT_TOKEN=
# 자동 공지가 필요한 서버만 Discord에서 /setup을 실행합니다.

TIME_ZONE=Asia/Seoul
DAILY_RELEASE_HOUR=9
READING_MINUTES=30
WRITING_MINUTES=20

# Official Codex SDK uses the server operator's codex login session.
CODEX_MODEL=gpt-5.6-terra
CODEX_TIMEOUT_SECONDS=180

# No category restriction: sample arXiv across all subjects.
# Automatic import only admits CC BY or CC0 papers whose license was verified.
ARXIV_ENABLED=true
ARXIV_POOL_TARGET=7
ARXIV_LOOKBACK_DAYS=365
ARXIV_USER_AGENT=PaperLeague/0.1 (private daily research reading app)

# 수집 후보와 일일 추첨에서 논문 1편당 적용하는 상대 가중치 (각각 1~100).
# arXiv 게재/채택 표기 기준이며, 기본 우선 목록은 여러 분야의 학회·저널을 포함합니다.
PAPER_WEIGHT_PREFERRED=5
PAPER_WEIGHT_PUBLISHED=2
PAPER_WEIGHT_UNCONFIRMED=1
# 생략하면 기본 목록. 지정하면 쉼표로 구분한 이름으로 우선 목록 전체를 교체합니다.
# PAPER_PREFERRED_VENUES=NeurIPS,ICML,ICLR,CVPR,Nature,Science,Physical Review Letters,Annals of Mathematics
